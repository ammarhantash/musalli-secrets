# Copy this file to .env and fill in your real MongoDB Atlas credentials.
# Get these from MongoDB Atlas: Database Access (user/password) and Connect → Drivers (URI).

MONGODB_USERNAME=""
MONGODB_PASSWORD=""
MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net"
MONGODB_DB="musallis"
